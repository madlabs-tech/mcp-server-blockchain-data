/target
.env
config/config.toml
config/secrets.toml
config/admin_token
config/dashboard_password
*.db
*.db-wal
*.db-shm
/data

# local planning docs only (not shipped)
/plan/

# agent worktrees (local only)
/.claude/worktrees/
