/target
.env
config/config.toml
config/secrets.toml
config/admin_token
*.db
*.db-wal
*.db-shm
/data
/crates/*/data

# local planning docs only (not shipped)
/plan/

# agent worktrees (local only)
/.claude/worktrees/
